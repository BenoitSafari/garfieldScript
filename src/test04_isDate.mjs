import GLaDOS from '../app/classes/GLaDOS.mjs';
const glados = new GLaDOS();

/**
 * Créer une function qui prend en paramètre une chaine de caractères et qui retourne true ou false s'il s'agit d'une date valide ou non.
 * - La valeur en sortie doit être de type "boolean"
 */
function isDate() {
    return 'NOT_IMPLEMENTED';
}

glados.testIsDate(isDate);
